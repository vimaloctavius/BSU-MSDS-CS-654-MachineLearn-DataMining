And then you will have each parameter defined and corresponding passing argument to it and then the function will perform what it when you need it to do OK. Alright so the next is the dictionary actually OK so let's let's show the dictionary OK. So I will see a more or less operations and functions in other future classes and now you have a dictionary right here OK so we have dictionary DICT and then we have four pairs and four keys John, Tom, Susan and Helen and values after the colon right OK and then so this we have a size of four four pairs basically. Now let's try to get to the key values in this dictionary. So OK in DICT.keys so the dictionary variable keys will return the key values in the dictionary OK so let's print OK so let's do this first OK now if we want to print the values then we say for K in DICT.keys. So basically it's just provide the dictionary data structure first and the key value next and then the corresponding values will be all printed OK right and of course I remember that we can also do the print DICT.values so this is also a useful function to get to the values of a dictionary OK right OK sometimes it might be useful for your program to get to receive the input from the user directly or dynamically and in that case we have something called input let me show this first OK so input is a function that will expect something receive or receive expect something to be received from the screen or from the keyboard actually right so let's do this OK so we say all right so so what's your choice one two or three and this is for this is for demonstration let's say we have a choice three OK and remember here you don't do the you don't click the run you have to enter the enter key from your keyboard OK all right so now if we print the option OK so the option is three basically OK all right so for instance you have a menu consisting of several sub functions or sub choices and you can combine this option with a few if else or elif statements conditions actually and then I'll help you to know which option in the user has chosen and then you can pass this to another function in to do something follows basically right OK OK sometimes we need to read a data from a text file OK and so usually what we are going to perform will be something like this we're going to open the file and it's a read only OK and then we are going to read we can read a file line by line and we are going to save each line into a data structure in this case I will be in the list OK so let me demonstrate this first and don't forget to close your variable file variable if you will in this case OK so and then if we show data raw OK OK so you can check the sample input txt in the website but you can take a guess right here basically is a very simple text file three lines and the first line 30 10 40 25 and 33 let me see if I can move the file here to show you because it's a very small file we probably can let me see you can do it simple input txt all right OK so it's like this maybe clean the screen a little bit right so basically is this I'm just demonstrate that if you have a text file we like to process the data we can read the data line by line first and then you probably need to do some cleaning which we're going to demonstrate in the near future but right now it's very simple that you can read data from a text file so to speak OK and you can see this a backslash n less a new line basically and then that's also called a hidden character meaning you don't see this you don't see that and I'm sorry the hidden backslash n right here OK so but when you read the file you become part of your data structure storage basically so in that case we can use a function called strip OK so it's called strip OK strip is a common way to strip some non-needed characters if you will and if you don't provide anything to the function by default you cannot strip spaces and spaces including you know backslash n so to speak okay so let me show you this so after this we are going to just show the data int all right okay so